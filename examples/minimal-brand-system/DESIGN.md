# Minimal Brand System Example

This example shows the smallest useful implementation of the standard:

- one token set
- one logo manifest
- one asset doc page

Run validation against it with:

```bash
npm run validate -- --root examples/minimal-brand-system
```
